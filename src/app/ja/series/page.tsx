import { LocalizedSeriesPage, localizedSeriesMetadata } from "../../_localized/pages";

export const metadata = localizedSeriesMetadata("ja");

export default function Page() {
  return <LocalizedSeriesPage locale="ja" />;
}
