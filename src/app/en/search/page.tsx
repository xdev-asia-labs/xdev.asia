import { LocalizedSearchPage } from "../../_localized/pages";

export const metadata = {
  title: "Search",
  description: "Search posts, series and content on xDev",
};

export default function Page() {
  return <LocalizedSearchPage locale="en" />;
}
